<!DOCTYPE html>
<html>

<head>
  <%= stylesheet_link_tag "application" %>
</head>

<main>
  <div class="search-bar">
    <h2>Search by breed</h2>
    <%= form_tag(dogs_path, method: :get) do %>
    <%= text_field_tag :search, params[:search] %>
    <% submit_tag "Search", name: nil %>
    <button class="button2hover">Search</button>
    <% end %>
  </div>
  <div class="card-container">
    <% if @dogs.empty> %>
      <%= "There are currently no dogs by that breed for adoption." %>
    <% else %>
      <% @dogs.each do |dog| %>
        <div class="card" style="width: 18rem;">
          <div class="card-body">
            <img class="card-img-top" src="<%=dog.image_url%>" alt="dog" height="200" width="200">
            <div class="overlay">
              <h5 class="card-text"><%= link_to dog.name, dog_path(dog) %></h5>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</main>

<div id="footer">
  <p>&copy; Copyright 2019</p>
</div>

</html>
